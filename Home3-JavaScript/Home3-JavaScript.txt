1.Функции нужны для для работы определенного кусочка кода и если нам он пригодится несколько раз не пришлось его писать снова и снова,
а просто вызвать саму функцию.

2.Если мы заранее знаем, что в функции нам нужны определенные параметры мы их записываем и при необходимости меняем, а так же, 
чтобы для определенных действий нужно было создавать кучу других переменных и менять саму функцию